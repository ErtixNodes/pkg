pkgname=bttmtest
pkgver=1.0.0
pkgrel=0
pkgdesc="Test program for ertixnodes"
url="https://ertixnodes.xyz"
arch="all"
options="!check"
license="custom"

build() {
	echo "Building at $(pwd) with $pkgdir"

	echo "This is just a sample package used for testing. Please refer to the other APKBUILD files for the real packages!"
}
package() {
	echo "Package (not needed)"

	mkdir -p "$pkgdir"
	echo "Pkdir created"
}
